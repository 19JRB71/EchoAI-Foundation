Here's Prompt 25: Full System Testing, Bug Fixes, and Production Deployment for EchoAI:
EchoAI is an AI-powered SaaS marketing platform. You've built the complete system across 24 prompts. Now you need to do a full end to end test of every feature, fix any bugs found, and prepare the platform for production so it's ready for your first real paying customer.
First run a complete system health check across all components. Test every API endpoint and confirm it returns the correct response. Test the database connections and confirm all 16 migrations are applied correctly. Test the Stripe webhook by simulating a payment success and payment failure event. Test the Facebook OAuth flow end to end. Test the Anthropic API connection by generating a short brand discovery response. Test the OpenAI API connection by transcribing a short audio clip and generating a short speech response. Test the email system by sending a test email to the admin address. Test the push notification system by sending a test push to a registered device.
Second fix any bugs or errors found during testing. Pay special attention to error handling on all API routes, making sure every route returns a proper JSON error response instead of crashing. Check that all environment variables are properly loaded and that missing variables fail gracefully with a clear error message rather than a cryptic crash. Verify that the admin seeder runs correctly and creates the admin account. Verify that the scheduler starts correctly and logs confirmation.
Third prepare for production. Add a production build script that builds the React client, runs all database migrations in order, seeds the admin account, and starts the server. Create a comprehensive README file that documents every feature, every API endpoint, every environment variable needed, how to run the migrations, how to start the server in development and production, and how to connect each third party service. Add proper CORS configuration that restricts API access to your domain only in production. Add rate limiting to all API routes to prevent abuse. Add request logging so you can see every request hitting your server in production.
Fourth do a final visual review of the complete dashboard. Check every page renders correctly. Check mobile responsiveness on all sections. Fix any layout issues, spacing problems, or broken styles. Make sure the landing page looks sharp and professional. Make sure the onboarding flow works from start to finish.
Output a final system status report showing every feature and whether it is working correctly.