"Railway deployment is now passing the build and deploy steps but failing on the Network Healthcheck. Initialization, Build, and Deploy all show green checkmarks but the healthcheck fails after 1 minute 31 seconds. This usually means the server is not binding to the correct port or crashed during startup due to a missing environment variable. Please check two things. First make sure server.js listens on process.env.PORT and not a hardcoded port like 5000 since Railway assigns its own port dynamically. Second check if there are any required environment variables that would cause the server to crash on startup if missing. List what environment variables are absolutely required for the server to start."
Also James — go to Railway, click on your EchoAI-Foundation service, click Variables tab, and make sure you have added all your environment variables there. That is likely the issue — the server is starting but crashing because it cannot find JWT_SECRET or DATABASE_URL.
Have you added your environment variables to Railway yet?James I can see the Variables tab. You only have 5 variables added so far:

NODE_ENV ✅
JWT_SECRET ✅
SESSION_SECRET ✅
ENCRYPTION_KEY ✅
DATABASE_URL ✅

Plus 7 variables added by Railway automatically.
The problem is you are missing ALL your API keys. The server is starting but crashing because it cannot find critical variables like ANTHROPIC_API_KEY, ELEVENLABS_API_KEY, OPENAI_API_KEY and others.
You need to add all these variables. Click New Variable for each one and add them:

ANTHROPIC_API_KEY
OPENAI_API_KEY
ELEVENLABS_API_KEY
ELEVENLABS_VOICE_ID — oG50Fr4tgJOLkpFhm2Ng
YOUTUBE_API_KEY
STRIPE_SECRET_KEY
STRIPE_PUBLISHABLE_KEY
FACEBOOK_APP_ID
FACEBOOK_APP_SECRET
FACEBOOK_ACCESS_TOKEN
ADMIN_EMAIL — jamesrblacketer71@gmail.com
ADMIN_PASSWORD
ALLOWED_ORIGINS

You can find all these values in your Replit Secrets tab. Go back to Replit, open Secrets, and copy each one over to Railway.
Add them one by one clicking New Variable each time. Once all are added Railway will automatically redeploy and the healthcheck should pass.
Do you want to start adding them now?